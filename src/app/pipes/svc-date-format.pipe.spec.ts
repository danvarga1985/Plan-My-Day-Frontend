import { SvcDateFormatPipe } from './svc-date-format.pipe';

describe('SvcDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SvcDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
